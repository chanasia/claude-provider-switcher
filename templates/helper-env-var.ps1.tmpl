# Auto-generated by claude-provider-switcher for profile '{{NAME}}' - DO NOT EDIT.
# Prints the API key from the environment variable '{{VAR}}' to stdout.
# Falls back from Process to User to Machine scope so keys set via the
# Windows environment UI work even when the launching shell predates them.
# This file contains NO secret material.
$ErrorActionPreference = 'Stop'

$secret = [Environment]::GetEnvironmentVariable('{{VAR}}', 'Process')
if ([string]::IsNullOrEmpty($secret)) {
    $secret = [Environment]::GetEnvironmentVariable('{{VAR}}', 'User')
}
if ([string]::IsNullOrEmpty($secret)) {
    $secret = [Environment]::GetEnvironmentVariable('{{VAR}}', 'Machine')
}
if ([string]::IsNullOrEmpty($secret)) {
    [Console]::Error.WriteLine("claude-provider-switcher: environment variable '{{VAR}}' is not set (Process/User/Machine)")
    exit 1
}
[Console]::Out.Write($secret)
exit 0
