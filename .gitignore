# Test artifacts
TestResults.xml
coverage.xml

# Editor / OS noise
.vscode/
*.swp
Thumbs.db
Desktop.ini

# Never commit local Claude settings (belt & suspenders — plugin writes outside repo anyway)
.claude/settings.local.json
